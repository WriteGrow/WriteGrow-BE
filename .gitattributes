# 컨테이너 안에서 실행되는 스크립트는 반드시 LF 여야 한다.
# CRLF 로 체크아웃되면 `#!/bin/bash\r` 가 되어 실행이 실패한다.
*.sh text eol=lf

# YAML 도 도구에 따라 CR 을 그대로 값에 포함시키는 경우가 있어 LF 로 고정한다.
*.yml text eol=lf
*.yaml text eol=lf

*.jar binary
